# Linux Training Platform - Requirements
# Основные зависимости времени выполнения

# В данном проекте используются только стандартные библиотеки Python
# Никаких внешних зависимостей не требуется

# Минимальная версия Python: 3.6+
# Используемые стандартные модули:
# - json (для работы с данными)
# - os (для системных операций)
# - datetime (для работы с датами)
# - random (для случайного выбора заданий)
# - re (для регулярных выражений)
# - smtplib (для отправки email, опционально)

# Опциональные зависимости для разработки (установка через pip install -e .[dev]):
# pytest>=6.0      # Для тестирования
# black             # Для форматирования кода  
# flake8            # Для линтинга
# twine             # Для публикации на PyPI