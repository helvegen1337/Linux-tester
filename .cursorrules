# Cursor Rules для Linux Training Platform

## 🎯 Контекст проекта
Это интерактивный тренажер для изучения Linux командной строки, разработанный для команды Artix. Проект включает обучающие модули, тесты, сценарии и систему отслеживания прогресса.

## 📁 Структура проекта
- `artix_training.py` - основной скрипт с UI и логикой
- `training_data.json` - все обучающие материалы и тесты  
- `user_progress.json` - прогресс пользователей (игнорируется git)
- `README.md` - документация пользователя
- `TECHNICAL_DOCS.md` - техническая документация

## 🐍 Python код
### Стиль кодирования:
- Используйте docstrings для всех функций
- Применяйте type hints где возможно
- Следуйте PEP 8 для форматирования
- Комментарии на русском языке для локального контекста
- Используйте осмысленные имена переменных

### Цветовая схема:
```python
# Всегда используйте класс Colors для цветного вывода:
Colors.HEADER   # Заголовки
Colors.CYAN     # Задания и вопросы  
Colors.YELLOW   # Пользовательский ввод
Colors.OKGREEN  # Успех
Colors.FAIL     # Ошибки
Colors.WARNING  # Предупреждения
```

### Обработка ошибок:
- Всегда используйте try/except для работы с файлами
- Логируйте ошибки через функцию `log_action()`
- Предоставляйте понятные сообщения об ошибках пользователю

## 📝 JSON структура данных
### training_data.json:
```json
{
  "модуль_id": {
    "name": "Название модуля",
    "commands": {
      "команда_id": {
        "name": "команда - описание",
        "theory": "Теоретическое описание",
        "when_useful": "Когда применять",
        "params": "Параметры команды",
        "practice": [
          {
            "task": "Описание задания",
            "solution": "правильное решение",
            "explanation": "Объяснение",
            "difficulty": 1-5
          }
        ]
      }
    }
  },
  "tests": {
    "тест_id": {
      "name": "Название теста",
      "questions": [
        {
          "question": "Текст вопроса",
          "options": ["вариант1", "вариант2", "вариант3", "вариант4"],
          "correct": индекс_правильного_ответа,
          "explanation": "Объяснение ответа"
        }
      ]
    }
  }
}
```

### Принципы добавления контента:
- Задания должны быть практичными и реальными
- Каждое задание должно иметь четкое объяснение
- Сложность заданий должна возрастать постепенно
- Включайте различные сценарии использования команд

## 🧪 Тестирование
- Новые функции должны быть протестированы вручную
- Проверяйте корректность JSON после изменений
- Тестируйте все пути выполнения в UI
- Убеждайтесь, что цветовая схема работает корректно

## 📊 Система прогресса
### Отслеживание прогресса:
- Используйте функции `load_user_progress()` и `save_user_progress()`
- Обновляйте прогресс только при успешном выполнении заданий
- Логируйте все действия пользователя

### Достижения:
- Добавляйте новые достижения в словарь `ACHIEVEMENTS`
- Проверяйте достижения после каждого изменения прогресса
- Показывайте уведомления о новых достижениях

## 🎨 UI/UX принципы
- Используйте рамки и отступы для структурирования вывода
- Применяйте цвета осмысленно (зеленый=успех, красный=ошибка)
- Предоставляйте четкие инструкции пользователю
- Используйте прогресс-бары для визуализации
- Всегда предоставляйте способ выхода (0, exit, quit)

## 🔧 Техническое обслуживание
### Логирование:
```python
log_action("Описание действия", "CATEGORY")
# Категории: INFO, SUCCESS, WARNING, ERROR
```

### Добавление новых модулей:
1. Добавьте модуль в `training_data.json`
2. Обновите иконки модулей в коде
3. Добавьте модуль в систему прогресса
4. Обновите документацию

### Добавление новых тестов:
1. Добавьте тест в секцию "tests"
2. Обновите функцию `run_test_session()`
3. Добавьте тест в `show_user_progress()`
4. Убедитесь в правильности нумерации

## 🚀 Развертывание
- Проект должен работать без дополнительных зависимостей
- Файлы данных должны быть в UTF-8
- Проверяйте совместимость с разными ОС

## ⚠️ Важные замечания
- НЕ изменяйте структуру существующих данных без миграции
- НЕ удаляйте существующие задания без причины
- ВСЕГДА тестируйте изменения в JSON на валидность
- СОХРАНЯЙТЕ обратную совместимость с прогрессом пользователей

## 🎓 Педагогические принципы
- От простого к сложному
- Практические примеры из реальной работы
- Понятные объяснения с контекстом
- Мотивирующая система достижений
- Возможность повторного прохождения материала

Следуйте этим правилам при работе с проектом для поддержания качества и консистентности кода.